services:
  lms-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lms-api
    ports:
      - "5000:5000"
    env_file:
      - .env.develop
    environment:
      NODE_ENV: production
      PORT: 5000
    restart: unless-stopped
