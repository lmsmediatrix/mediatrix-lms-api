version: "3.8"
services:
  app:
    build: .
    ports:
      - "${PORT}:${PORT}"
    environment:
      - NODE_ENV=development
      - PORT=5000
    restart: always
    volumes:
      - .:/app
      - /app/node_modules
